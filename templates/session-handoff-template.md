# Session Handoff Note (added 0.1.1)

Use this to close out a session before context runs out or the
session ends, so the next session — possibly a different LLM
entirely — can resume from this file instead of from chat history.
See `skills/free-tier-llm-operating-model/SKILL.md` and
`commands/save-progress.md`.

## Task
<What was being worked on — link the story/bug/workflow stage>

## Agent/workflow in progress
<Which DEK agent and workflow stage was active>

## Done so far
<Concrete, verifiable progress — files changed, decisions made,
tests written>

## Next step
<The single next action — specific enough that a new session doesn't
need to guess>

## Open questions / blockers
<Anything unresolved that the next session needs to know about before
continuing>

## Relevant artifacts
<Links/paths to the requirements note, ADR, API contract, or other
files the next session will need to load>
