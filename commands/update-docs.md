# Command: update-docs

**Invokes:** `tech-writer`
**Use when:** a feature/fix has shipped, or documentation has gone
stale.

## Instruction
Run the documentation portion of
`workflows/documentation-and-maintenance.md`, using
`documentation-standards`.

## Output
Updated README/API docs/runbooks reflecting current behavior, plus
release notes if this follows a deployment.
